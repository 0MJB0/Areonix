enum WidgetSize {
  // Button
  gradientButton(30),
  // Card
  cardRadius(20),
  cardBorderWidth(2),
  // Padding
  paddingLow(8),
  // Icon
  icon(20),
  // Dialog
  dialogRadius(12),
  dialogWidth(2),
  // Dropdown
  dropdownWidth(2),
  dropdownRadius(12),
  dropdownMaxHeight(15),
  // Row
  rowRadius(8),
  rowImageHeight(15),
  rowImageWidth(15),
  // Textfield
  textfieldRadius(10),
  textfieldWidth(2),
  textfieldGapPadding(1),
  ;

  final double value;

  const WidgetSize(this.value);
}
